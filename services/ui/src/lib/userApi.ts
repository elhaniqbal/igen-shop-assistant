import { http } from "./httpClient";

// Placeholder for user workflow endpoints later.
// Keep this file so user-side components import from here.

export const userApi = {
  // cardAuth: (card_id: string) => http<...>("/rfid/card-auth", { method:"POST", body: JSON.stringify({card_id}) }),
  // dispense: (...) => http<...>("/dispense", ...),
  // etc.
};
