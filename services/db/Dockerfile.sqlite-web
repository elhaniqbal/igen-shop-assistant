# Dockerfile.sqlite-web
FROM python:3.11-slim
RUN pip install --no-cache-dir sqlite-web
EXPOSE 8080
# Path comes from your env: SQLITE_PATH=/data/local_cache.db
ENV SQLITE_DATABASE=/data/local_cache.db
CMD sh -lc 'sqlite_web --host 0.0.0.0 --port 8080 -x "$SQLITE_DATABASE"'
