# Pytest suite for the IGEN Shop Assistant backend

Copy this folder into `services/backend/` so the structure becomes:

- `services/backend/tests/`
- `services/backend/pytest.ini`
- `services/backend/requirements-dev.txt`

Then install dev dependencies and run `pytest`.

This suite focuses on the FastAPI backend, database flows, MQTT event handlers, and the legacy bridge serial protocol parser.
